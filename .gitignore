# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*
terraform.tfstate.backup

# Ignore Terraform lock files
.terraform.lock.hcl

# Ignore all .tfvars files, which might contain sensitive data, such as
# passwords, private keys, and other secrets.
*.tfvars
*.tfvars.json

# Ignore the workspace files like .DS_Store (for macOS)
.DS_Store

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore any .zip files as they usually contain compiled code or binaries
*.zip
